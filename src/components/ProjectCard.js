import React from 'react';

function ProjectCard(props) {
    return (
        <div className="card-container">
            {/* create a card for the project like normal but pass props in for the image, links, etc.*/}
            {/* hover to seer the links and project name? 
            IMPORTANT! -> (Look into if that meets accessibility standards.) */}
        </div>
    );
}

export default ProjectCard;